name: Prueba

on:
  push:
    branches:
      - main
      
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: saludo
        run: |
          echo "hola mundo!"
      - name: fecha
        run: date